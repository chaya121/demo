# แผนระบบ Authentication, Role-Based Access Control และแพลตฟอร์มหลายแอป

> เอกสารนี้เป็น **แผนระยะยาว** (planning doc) ยังไม่มีการเริ่มพัฒนา — เขียนไว้เพื่อกำหนดทิศทาง, ออกแบบโครงสร้างเบื้องต้น, และรวบรวมคำถามที่ต้องตัดสินใจก่อนลงมือทำจริง

## 1. เป้าหมาย (Vision)

ปัจจุบัน "Apparel Creations" เป็นแอปเดี่ยวสำหรับบันทึกใบขั้นตอนการผลิต **ไม่มีระบบยืนยันตัวตนเลย** ใครมีลิงก์ก็เข้าถึง แก้ไข หรือลบข้อมูลทั้งหมดได้ทันที

เป้าหมายในอนาคต:
1. **มีระบบลงทะเบียน/login** — รู้ว่าใครเป็นคนทำอะไรกับข้อมูล
2. **มีการกำหนดสิทธิ์ตามประเภทบัญชี (Role-Based Access Control)** — แต่ละบทบาททำได้/ทำไม่ได้ต่างกัน
3. **ขยายเป็นแพลตฟอร์มที่รวมหลายแอป** — ระบบบันทึกการผลิตเป็นแค่ "โมดูลแรก" ในระบบที่ใหญ่ขึ้น โดยใช้บัญชีผู้ใช้และสิทธิ์ชุดเดียวกัน (single sign-on ภายในองค์กร) แต่ละแอปย่อยมี role/สิทธิ์เป็นของตัวเอง

> **หมายเหตุ:** เคยพิจารณาย้ายฐานข้อมูลออกจาก Supabase ไปโฮสต์เองบน NAS ของโรงงาน แต่**ยกเลิกแผนนี้แล้ว** เนื่องจากต้นทุนฮาร์ดแวร์สูงและความเสถียรขึ้นกับเน็ต/ไฟที่โรงงานเองมากเกินไปเมื่อเทียบกับ Supabase — **ตัดสินใจใช้ Supabase ต่อไป** ดูการติดตามความจุที่ข้อ 8

## 2. สถานะปัจจุบัน (ที่เกี่ยวข้องกับแผนนี้)

- Backend: Express + PostgreSQL (Supabase) ไม่มี auth middleware ใดๆ ทุก endpoint เปิดสาธารณะ
- Frontend: React ไม่มี router (สลับหน้าด้วย local state `activeTab` ใน `App.jsx`), ไม่มีแนวคิดเรื่อง "ผู้ใช้ที่ login อยู่" เลย
- ข้อมูลถูกเก็บแบบ JSONB blob ต่อ record ไม่มีการอ้างอิงว่า "ใครสร้าง/แก้ไขล่าสุด"
- มีผู้ใช้งานจริงแล้ว (Mer, ฝ่ายผลิต, ฝ่ายขาย) กำลังกรอกข้อมูลจริงในระบบ

## 3. Roadmap แบ่งเฟส

การขึ้นระบบ auth+RBAC+multi-app ทีเดียวมีความเสี่ยงสูงและใช้เวลานาน จึงแบ่งเป็นเฟสที่แต่ละเฟสใช้งานได้จริงในตัวเอง:

| เฟส | เป้าหมาย | ผลลัพธ์ |
|---|---|---|
| **Phase 1** | Authentication พื้นฐาน | ต้อง login ก่อนถึงเข้าแอปนี้ได้ (ปิดช่องโหว่วิกฤตที่มีอยู่ตอนนี้) |
| **Phase 2** | Role-Based Access Control ในแอปนี้ | แต่ละบทบาทเห็น/ทำได้ไม่เท่ากันตามสิทธิ์ |
| **Phase 3** | โครงสร้างรองรับหลายแอป (Platform) | มี "เปลือกแอป" กลาง, บัญชีเดียวใช้ข้ามแอปได้, สิทธิ์แยกตามแอป |
| **Phase 4** | แอปใหม่เพิ่มเติมบนแพลตฟอร์ม | เพิ่มโมดูลอื่นๆ ตามที่ธุรกิจต้องการ |

**แนะนำ:** เริ่ม Phase 1 ก่อนโดยไม่ต้องรอออกแบบ Phase 3-4 ให้เสร็จ เพราะ Phase 1 แก้ปัญหาความเสี่ยงเร่งด่วนที่สุด (ข้อมูลจริงไม่มีการป้องกันเลย) ส่วน Phase 3-4 ค่อยออกแบบละเอียดตอนใกล้ถึงจริง เนื่องจากตอนนี้ยังไม่รู้ว่าแอปอื่นๆ ที่จะเพิ่มคืออะไร

## 4. ออกแบบระบบ (Technical Design)

### 4.1 แนวคิดหลัก: ผู้ใช้ 1 คน หลายแอป หลายสิทธิ์

ออกแบบให้ "ผู้ใช้" กับ "แอป" แยกออกจากกันตั้งแต่ต้น แม้ Phase 1-2 จะมีแอปเดียว เพื่อไม่ต้องรื้อโครงสร้างตอนขึ้น Phase 3:

```
users (บัญชีผู้ใช้ – ใช้ร่วมกันทุกแอป)
  └── user_roles (บัญชีนี้มี role อะไรในแอปไหนบ้าง)
        └── roles (role ผูกกับแอปใดแอปหนึ่งเสมอ)
              └── apps (ทะเบียนแอป/โมดูลทั้งหมดในระบบ)
```

### 4.2 โครงสร้างตาราง (ร่างเบื้องต้น)

```sql
-- บัญชีผู้ใช้ ใช้ร่วมกันทุกแอปในแพลตฟอร์ม
CREATE TABLE users (
  id SERIAL PRIMARY KEY,
  email TEXT UNIQUE NOT NULL,
  password_hash TEXT NOT NULL,        -- bcrypt/argon2 hash เท่านั้น ห้ามเก็บ plaintext
  display_name TEXT NOT NULL,
  is_active BOOLEAN NOT NULL DEFAULT true,
  created_at TIMESTAMP DEFAULT NOW()
);

-- ทะเบียนแอป/โมดูลทั้งหมด (Phase 1-2 มีแถวเดียว)
CREATE TABLE apps (
  id SERIAL PRIMARY KEY,
  code TEXT UNIQUE NOT NULL,          -- เช่น 'production-tracker'
  name TEXT NOT NULL                  -- เช่น 'ระบบบันทึกการผลิต'
);

-- role ผูกกับแอปใดแอปหนึ่งเสมอ (สิทธิ์ต่างแอปไม่ปนกัน)
CREATE TABLE roles (
  id SERIAL PRIMARY KEY,
  app_id INTEGER NOT NULL REFERENCES apps(id),
  code TEXT NOT NULL,                 -- เช่น 'admin', 'mer', 'production', 'sales', 'viewer'
  name TEXT NOT NULL,
  UNIQUE (app_id, code)
);

-- ผู้ใช้ 1 คนมีได้หลาย role หลายแอป
CREATE TABLE user_roles (
  user_id INTEGER NOT NULL REFERENCES users(id),
  role_id INTEGER NOT NULL REFERENCES roles(id),
  PRIMARY KEY (user_id, role_id)
);

-- session store (ถ้าเลือกใช้ session-based auth ตามข้อ 4.3)
-- ใช้ schema มาตรฐานของ connect-pg-simple
```

> ตาราง `records`/`master` เดิมอาจต้องเพิ่มคอลัมน์ `created_by` / `updated_by` (FK ไป `users.id`) เพื่อรู้ว่าใครทำอะไร — เป็นพื้นฐานของ audit log ในอนาคตด้วย

### 4.3 กลไก Authentication

**ข้อเสนอ:** Session-based auth ด้วย cookie (`httpOnly`, `secure`, `sameSite`) เก็บ session ไว้ใน Postgres ผ่าน `connect-pg-simple`

เหตุผลที่เลือกแบบนี้แทน JWT:
- ทีมงานมีขนาดเล็ก ไม่ต้องการ stateless auth ข้ามหลาย service
- Revoke สิทธิ์ได้ทันที (ลบ session แถวเดียว) ต่างจาก JWT ที่ต้องรอหมดอายุหรือทำ blacklist เพิ่ม
- ไม่ต้องจัดการ refresh token ให้ซับซ้อน

**Password hashing:** bcrypt (cost factor 10-12) — ห้าม implement เอง ใช้ library `bcrypt`/`bcryptjs`

**นโยบายการสร้างบัญชี (ตัดสินใจแล้ว):** ไม่เปิดให้สมัครเอง — **เฉพาะ Admin เท่านั้นที่สร้างบัญชีผู้ใช้ได้** จึงไม่มี endpoint `register` แบบสาธารณะ

**Endpoint ที่ต้องเพิ่ม:**
- `POST /api/admin/users` — Admin สร้างบัญชีใหม่ (ตั้งอีเมล, ชื่อ, role เริ่มต้น, รหัสผ่านเริ่มต้น/ชั่วคราว) ต้องผ่าน `requireAuth` + `requireRole('admin')`
- `POST /api/auth/login`
- `POST /api/auth/logout`
- `GET /api/auth/me` — คืนข้อมูลผู้ใช้ปัจจุบัน + role ที่มีในแอปนี้
- `POST /api/auth/change-password` — ให้ผู้ใช้เปลี่ยนรหัสผ่านที่ Admin ตั้งให้ตอนแรก (แนะนำให้บังคับเปลี่ยนตอน login ครั้งแรก)

### 4.4 Middleware และการป้องกัน endpoint เดิม

เพิ่ม middleware 2 ชั้นใน `backend/index.js`:
1. `requireAuth` — เช็คว่ามี session ที่ login อยู่ ไม่งั้นตอบ 401
2. `requireRole(...allowedRoleCodes)` — เช็คว่า role ของผู้ใช้ในแอปนี้อยู่ในลิสต์ที่อนุญาต ไม่งั้นตอบ 403

ตัวอย่างการใช้กับ endpoint ที่มีอยู่แล้ว:
```js
app.delete('/api/clear', requireAuth, requireRole('admin'), async (req, res) => { ... });
app.put('/api/master', requireAuth, requireRole('admin', 'mer'), async (req, res) => { ... });
app.post('/api/records', requireAuth, async (req, res) => { ... }); // ทุก role ที่ login แล้วทำได้
```

### 4.5 ร่างตาราง Role & สิทธิ์สำหรับแอปนี้ (ต้องคุยกันก่อน — ดูข้อ 7)

| การกระทำ | Admin | Mer | ฝ่ายผลิต | ฝ่ายขาย | Viewer |
|---|:---:|:---:|:---:|:---:|:---:|
| สร้าง/แก้ไขใบขั้นตอนการผลิต | ✅ | ✅ | เฉพาะช่อง "ผลิตจริง" | ❌ | ❌ |
| ลบใบขั้นตอนการผลิต | ✅ | ✅ | ❌ | ❌ | ❌ |
| ดูประวัติ / ดาวน์โหลด PDF | ✅ | ✅ | ✅ | ✅ | ✅ |
| แก้ไขข้อมูลหลัก (master data) | ✅ | ✅ | ❌ | ❌ | ❌ |
| ดูหน้าสถิติ | ✅ | ✅ | ✅ | ✅ | ✅ |
| ลบข้อมูลทั้งหมด (`/api/clear`) | ✅ | ❌ | ❌ | ❌ | ❌ |
| จัดการผู้ใช้ / กำหนดสิทธิ์ | ✅ | ❌ | ❌ | ❌ | ❌ |

ตารางนี้เป็น **ตัวอย่างร่าง** จากการสังเกตการใช้งานจริงในระบบเท่านั้น ยังไม่ใช่ของจริง — ต้องให้เจ้าของระบบยืนยัน/แก้ก่อนเริ่ม Phase 2

### 4.6 การเปลี่ยนแปลงฝั่ง Frontend

- ต้องเพิ่ม routing จริง (เช่น `react-router`) เพราะตอนนี้ไม่มี URL แยกหน้าเลย (สลับ tab ด้วย state ล้วนๆ) — จำเป็นสำหรับหน้า `/login` และการขยายเป็นหลายแอปในอนาคต
- เพิ่ม `AuthContext` เก็บสถานะผู้ใช้ที่ login อยู่ + role ปัจจุบัน
- หน้า Login/Register ใหม่
- ซ่อน/แสดงปุ่มและแท็บตาม role (เช่น ฝ่ายขายไม่เห็นแท็บ "ข้อมูลหลัก")
- Redirect ไปหน้า login อัตโนมัติถ้า session หมดอายุ (เช่น เจอ 401 จาก API)

## 5. ผลกระทบต่อข้อมูล/ผู้ใช้เดิม (Migration)

- ข้อมูล `records`/`master` ที่มีอยู่แล้วไม่มี `created_by` — ตอน migrate ต้องปล่อยเป็น `NULL` หรือ assign ให้ admin คนแรกเป็นเจ้าของแทน
- ต้องสร้างบัญชี admin คนแรกด้วยมือ (seed script) ก่อนเปิดใช้งานจริง ไม่งั้นจะไม่มีใคร login เข้าไปสร้างบัญชีอื่นต่อได้
- ช่วงเปลี่ยนผ่าน ควรมี "โหมดไม่บังคับ login" ชั่วคราวเพื่อให้ทีมงานทดสอบ ก่อนบังคับใช้จริงเต็มรูปแบบ

## 6. สิ่งที่ยังไม่ทำในแผนนี้ (Non-goals ตอนนี้)

- ยังไม่ออกแบบ "เปลือกแอปกลาง" (Phase 3) แบบละเอียด เพราะยังไม่รู้ว่าแอปอื่นที่จะเพิ่มคืออะไร
- ยังไม่รวม SSO กับระบบภายนอก (Google Workspace, Microsoft 365 ฯลฯ) — ถ้าต้องการค่อยเพิ่มทีหลังได้โดยไม่กระทบโครงสร้างตารางด้านบนมาก
- ยังไม่ทำ 2FA / audit log แบบละเอียด (แต่โครงสร้าง `created_by`/`updated_by` ในข้อ 4.2 เป็นฐานให้ต่อยอดได้)

## 7. คำถามที่ต้องตัดสินใจก่อนเริ่มพัฒนาจริง

**ตัดสินใจแล้ว:**
- ✅ **การสร้างบัญชีใหม่** — ไม่เปิดให้สมัครเอง เฉพาะ **Admin เท่านั้นที่สร้างบัญชีได้** (ดูรายละเอียดในข้อ 4.3)

**ยังต้องตัดสินใจ:**
1. **บทบาท (roles) ที่ต้องการจริงๆ มีอะไรบ้าง** — ตารางในข้อ 4.5 เป็นแค่ตัวอย่างจากการสังเกต ต้องยืนยัน/แก้
2. **ฝ่ายผลิตแก้ไขได้เฉพาะใบที่เกี่ยวข้องกับตัวเอง หรือแก้ได้ทุกใบ?** (มีผลต่อการออกแบบ permission ว่าต้องเช็คระดับ "record" ไม่ใช่แค่ระดับ "action")
3. **แอปอื่นๆ ที่วางแผนจะรวมเข้ามาในอนาคตคืออะไรบ้าง** (แม้จะยังไม่ทำตอนนี้ แต่รู้คร่าวๆ จะช่วยออกแบบ Phase 3 ได้แม่นขึ้น)
4. **ต้องการ SSO กับระบบอีเมล/บัญชีองค์กรที่มีอยู่แล้วไหม** หรือสร้างระบบ login แยกเป็นเอกเทศ
5. **Admin ตั้งรหัสผ่านเริ่มต้นให้ผู้ใช้เองตรงๆ หรือระบบสุ่มรหัสชั่วคราวแล้วบังคับเปลี่ยนตอน login ครั้งแรก?** (แนะนำแบบหลัง ปลอดภัยกว่า แต่ยังไม่ได้ยืนยัน)

## 8. ติดตามความจุฐานข้อมูล Supabase (แทนแผน NAS ที่ยกเลิกแล้ว)

**สถานะ:** ยกเลิกแผนย้ายไป NAS แล้ว — ต้นทุนฮาร์ดแวร์สูงและความเสถียรขึ้นกับเน็ต/ไฟที่โรงงานเองมากเกินไปเมื่อเทียบกับข้อดี **ตัดสินใจใช้ Supabase ต่อไป** ความกังวลตอนนี้คือเรื่องความจุจะพอไหมเมื่อข้อมูลเพิ่มขึ้น

### 8.1 ขนาดข้อมูลปัจจุบัน (เช็คจริงจากฐานข้อมูล ณ วันที่เขียนหัวข้อนี้)

| รายการ | ขนาด |
|---|---|
| ตาราง `records` (ข้อมูลใบขั้นตอนการผลิตทั้งหมด รวม 3 รายการ) | 624 KB |
| ตาราง `master` (ข้อมูลหลัก) | 104 KB |
| ใบที่**ไม่มี**รูปภาพแนบ | ~826 ไบต์/ใบ (เล็กมาก) |
| ใบที่**มี**รูปภาพแนบ 1 รูป | ~80-230 KB/ใบ |

**สรุป: รูปภาพคือตัวกินพื้นที่หลักเกือบทั้งหมด** ไม่ใช่ข้อมูลตัวหนังสือ

### 8.2 ทำไมรูปภาพถึงกินพื้นที่เยอะ

ระบบเก็บรูปเป็น base64 string ฝังอยู่ในคอลัมน์ `data` (JSONB) ของตาราง `records` โดยตรง ไม่ได้แยกไปเก็บที่ระบบเก็บไฟล์ต่างหาก การเข้ารหัส base64 ทำให้ขนาดใหญ่กว่าไฟล์รูปจริงประมาณ 33% โดยธรรมชาติ

### 8.3 ประมาณการเมื่อข้อมูลเพิ่มขึ้น

ถ้าเฉลี่ยใบละ ~150KB (รวมรูป 1 รูป) ทุกๆ **1,000 ใบงาน** จะใช้พื้นที่ประมาณ **150MB** — เทียบกับ Supabase free tier (500MB) ยังพอใช้ได้อีกหลายพันใบ, ถ้าเป็น Pro tier (8GB ขึ้นไป) ยิ่งเหลือเผื่อมาก แต่ตัวเลขนี้จะเพิ่มเร็วขึ้นถ้าแต่ละใบแนบรูปมากกว่า 1 รูป

### 8.4 สิ่งที่ควรทำ

1. **เช็ค plan/tier ปัจจุบันของบัญชี Supabase** ที่ Dashboard → Settings → Billing เพื่อรู้ขีดจำกัดที่แท้จริง (เอกสารนี้บอกได้แค่ขนาดข้อมูลจริงที่ใช้อยู่ ไม่มีสิทธิ์เข้าถึงข้อมูล billing ของบัญชี)
2. ถ้าใช้ free tier อยู่ แนะนำอัปเกรดเป็น Pro (~$25/เดือน) เมื่อข้อมูลเริ่มเยอะขึ้น เพราะ free tier จะ pause อัตโนมัติถ้าไม่มีการใช้งานเกิน 1 สัปดาห์ด้วย (เสี่ยงระบบล่มกะทันหันถ้าไม่มีคนเข้าใช้ในบางช่วง)
3. **ถ้าอยากประหยัดพื้นที่ + ให้เว็บโหลดเร็วขึ้นไปพร้อมกัน** — ย้ายรูปภาพออกจาก JSONB ไปเก็บใน **Supabase Storage** (ระบบเก็บไฟล์แยกต่างหาก คิดราคาต่อ GB ถูกกว่า database storage มาก) แล้วเก็บแค่ URL ไว้ในฟิลด์แทน เป็นแนวทางที่เคยแนะนำไว้ก่อนหน้านี้แล้วแต่ยังไม่ได้ทำ — ถ้าความจุเริ่มเป็นปัญหาจริง นี่คือจุดแรกที่ควรแก้ก่อนคิดเรื่องอัปเกรด plan

---
*เอกสารนี้จะอัปเดตต่อเมื่อมีการตัดสินใจในข้อ 7 หรือเมื่อเริ่มลงมือพัฒนา Phase 1 จริง*
